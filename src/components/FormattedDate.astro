---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="flex justify-center md:justify-start" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('id-ID', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
